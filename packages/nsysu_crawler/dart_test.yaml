exclude_tags: 'live || live-anonymous'

presets:
  live:
    exclude_tags: '__never__'
    include_tags: 'live'
    reporter: expanded
    concurrency: 1
  live-anonymous:
    exclude_tags: '__never__'
    include_tags: 'live-anonymous'
    reporter: expanded
    concurrency: 1

tags:
  live:
  live-anonymous:
  __never__:
