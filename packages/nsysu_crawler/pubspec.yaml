name: nsysu_crawler
description: Pure Dart crawler / API client for NSYSU school affairs.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  ap_common_core: ^1.0.1-dev.10

  cookie_jar: ^4.0.3
  crypto: ^3.0.1
  dio: ^5.0.0
  dio_cookie_manager: ^3.0.0
  html: ^0.15.0
  json_annotation: ^4.6.0
  sprintf: ^7.0.0

dev_dependencies:
  build_runner: ^2.1.5
  json_serializable: ^6.0.1
  lint: ^2.1.2
  test: ^1.24.0
